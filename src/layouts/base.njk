<!DOCTYPE html>
<html lang="{{ meta.lang or site.lang }}">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="color-scheme" content="only light" />
    <title>{{ site.title }} - {{ title or meta.title }}</title>

    <link rel="canonical" href="{{ page.url | url | absoluteUrl(site.baseUrl) }}" />
    <link rel="stylesheet" href="{{ '/dist/app.css' | url }}" />

    {% include "meta.njk" %}
    {% include "schema.njk" %}
  </head>

  <body class="parallax">
    <div class="wrapper">
    <!-- skiplinks -->
    <!-- <a href="#main" class="visually-hidden-focusable">Skip to main content</a> -->

    {% include "header.njk" %}

    <main id="main">
      <div class="container py-4">
        {{ content | safe }}
      </div>
    </main>

    {% include "footer.njk" %}
    <script type="text/javascript" src="{{ '/dist/app.js' | url }}"></script>
  </div>
  </body>
</html>
